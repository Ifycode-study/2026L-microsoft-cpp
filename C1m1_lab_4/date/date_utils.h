
#pragma once

#include <string>

// Returns the current system date and time as a formatted string
// Useful for logging or displaying timestamps to the user.
std::string getCurrentTimestamp();

// Calculates the number of days between two dates (placeholder logic)
int daysBetween(std::string date1, std::string date2);
