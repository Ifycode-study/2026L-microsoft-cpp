
#pragma once

// Generates a random integer between a specified minimum and maximum (inclusive)
// Used for games, simulations, or randomized UI elements.
int getRandomNumber(int min, int max);

// Generates a random boolean (true or false)
// Useful for "coin flip" logic or chance-based events.
bool flipCoin();
